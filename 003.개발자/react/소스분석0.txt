# 기술 로드맵 관리 시스템 - 완전 이해 학습 로드맵

## 프로젝트 개요
- **이름**: Technology Roadmap Management System (기술로드맵 관리시스템)
- **위치**: `C:\Users\jooladen\Downloads\roadmap-main-layout\just-do-it-main`
- **기술스택**: React 18 + TypeScript + Vite + shadcn/ui + Tailwind CSS
- **목적**: 기술 개발 계획을 4단계 계층구조로 관리하고 타임라인으로 시각화
- **현재상태**: Mock 데이터 사용, API 연동 준비 완료

---

## 🎯 학습 목표
이 프로젝트의 **전체 아키텍처, 데이터 흐름, 핵심 로직**을 완벽하게 이해하여, 새로운 기능을 추가하거나 버그를 수정할 수 있는 수준에 도달하기

---

## 📚 전체 학습 로드맵: 5단계 (총 3-4주 과정)

### **1단계: 프로젝트 기초 이해** (3-4일)
**목표**: 프로젝트가 무엇을 하는지, 어떻게 구성되어 있는지 파악

#### 읽을 파일 (순서대로):
1. ✅ `README.md` - 프로젝트 설명, 설치 및 실행 방법
2. ✅ `package.json` - 의존성 패키지, 스크립트 명령어
3. ✅ `vite.config.ts` - 빌드 설정 (포트, 플러그인, path alias)
4. ✅ `tailwind.config.ts` - 디자인 시스템 설정 (색상, 폰트, 애니메이션)
5. ✅ `src/index.css` - CSS 변수, 전역 스타일, 다크모드
6. ✅ `src/main.tsx` - 애플리케이션 진입점 (5줄)
7. ✅ `src/App.tsx` - 라우팅 설정, 앱 구조

#### 핵심 개념:
- Vite: 빠른 빌드 도구, HMR (Hot Module Replacement)
- Path Alias: `@/*` → `./src/*` (import 경로 단축)
- React Router: 클라이언트 사이드 라우팅
- QueryClient: 서버 상태 관리 준비

#### 실습:
- [ ] 프로젝트 실행: `npm install` → `npm run dev`
- [ ] 모든 페이지 둘러보기 (URL 직접 입력하며)
- [ ] 개발자 도구로 컴포넌트 구조 확인

---

### **2단계: 레이아웃 & 내비게이션 이해** (2-3일)
**목표**: 앱의 껍데기(Layout)와 탐색(Navigation) 시스템 파악

#### 읽을 파일:
1. ✅ `src/components/layout/MainLayout.tsx`
   - 전체 앱 구조: Header + Sidebar + Content
   - 모든 페이지가 이 레이아웃을 사용

2. ✅ `src/components/layout/Header.tsx`
   - 상단 네비게이션 탭
   - 검색 입력창
   - 알림 버튼, 사용자 프로필

3. ✅ `src/components/layout/AppSidebar.tsx` ⭐ **중요**
   - 사이드바 펼침/접힘 상태 관리
   - SidebarContext 사용법
   - 현재 활성 라우트 감지
   - 재귀적 NavItem 컴포넌트

4. ✅ `src/components/NavLink.tsx`
   - React Router의 NavLink 래퍼

#### 핵심 개념:
- **React Context API**: 사이드바 상태를 여러 컴포넌트에서 공유
- **useLocation hook**: 현재 URL 경로 감지
- **Composition Pattern**: MainLayout으로 페이지를 감싸는 패턴
- **재귀 컴포넌트**: NavItem이 자기 자신을 children으로 렌더링

#### 실습:
- [ ] Header의 탭을 클릭하며 라우팅 변화 관찰
- [ ] Sidebar 펼침/접힘 버튼 동작 이해
- [ ] 개발자 도구로 SidebarContext 상태 확인

---

### **3단계: 데이터 모델 & Mock 데이터** (2-3일)
**목표**: 앱에서 사용하는 데이터 구조와 타입 완벽 이해

#### 읽을 파일:
1. ✅ `src/types/classification.ts` ⭐ **핵심 타입**
   ```typescript
   ClassificationLevel = "대분류" | "중분류" | "소분류" | "기술분류"
   ClassificationItem, ClassificationCategory
   ```

2. ✅ `src/data/classificationData.ts`
   - 4단계 계층 구조 샘플 데이터
   - 트리 구조 이해 (parentId로 연결)

3. ✅ `src/data/timelineData.ts`
   - 타임라인 아이템 구조
   - 부서별 색상 코드
   - 제품 vs 기술 구분

4. ✅ `src/data/departmentData.ts`
   - 부서(Department) 구조
   - 사용자(User) 구조
   - 부서-사용자 관계

5. 나머지 데이터 파일들 (빠르게 훑어보기):
   - `unclassifiedTechPlanData.ts`
   - `permissionData.ts`
   - `permissionRequestData.ts`
   - `classificationHistoryData.ts`
   - `exportHistoryData.ts`
   - `techPlanHistoryData.ts`

#### 핵심 개념:
- **TypeScript Interface**: 데이터 구조의 계약
- **트리 구조**: parentId로 계층 관계 표현
- **Union Type**: 제한된 선택지 (예: 부서 타입)
- **Optional Properties**: `?` 기호의 의미

#### 실습:
- [ ] classificationData를 종이에 트리로 그려보기
- [ ] timelineData의 날짜 범위 계산해보기
- [ ] 새로운 Mock 데이터 항목 추가해보기

---

### **4단계: 주요 기능 페이지 분석** (5-7일)
**목표**: 각 페이지의 비즈니스 로직과 UI 상호작용 완벽 이해

#### 4-1. 분류 트리 페이지 (가장 단순)
📂 **읽을 파일**:
1. ✅ `src/pages/Index.tsx` - 홈페이지 (분류 트리 표시)
2. ✅ `src/components/classification/ClassificationTree.tsx` ⭐
   - 재귀적 트리 렌더링
   - 펼침/접힘 상태 관리
   - Badge 레벨별 색상 매핑
3. ✅ `src/components/classification/ClassificationFilters.tsx`
   - 검색, 필터링 UI

**핵심 로직**:
- 3단계 컴포넌트: CategorySection → MediumItem → SmallItem
- 각 레벨별로 다른 Badge variant
- 접힘 상태를 객체로 관리: `{ [id]: boolean }`

**실습**:
- [ ] 새로운 대분류 추가 버튼 동작 추적
- [ ] 검색 기능 구현 분석
- [ ] Badge 색상 변경해보기

---

#### 4-2. 통합 기술계획 타임라인 (가장 복잡) ⭐⭐⭐
📂 **읽을 파일**:
1. ✅ `src/pages/tech-plan/IntegratedTechPlan.tsx` ⭐ **가장 중요**
   - 2024-2029년 (72개월) 타임라인
   - 날짜를 픽셀 위치로 변환하는 알고리즘
   - 부서별 색상 바(bar) 렌더링
   - 재귀적 카테고리 렌더링

**핵심 알고리즘**:
```typescript
// 날짜 → 오프셋 (개월 수)
getMonthOffset(dateStr) {
  const [year, month] = dateStr.split("-").map(Number);
  return (year - startYear) * 12 + (month - 1);
}

// 시작/종료 → 바의 위치와 너비 (%)
getBarPosition(startDate, endDate) {
  const startOffset = getMonthOffset(startDate);
  const endOffset = getMonthOffset(endDate);
  const left = (startOffset / totalMonths) * 100;
  const width = ((endOffset - startOffset + 1) / totalMonths) * 100;
  return { left: `${left}%`, width: `${width}%` };
}
```

**부서별 색상 매핑**:
- SR: blue, MX: green, NW: purple, VD: orange, DA: pink
- 의료기기: red, 생기연: indigo, DPC: teal

**실습**:
- [ ] 2025년 3월부터 2026년 6월까지 항목의 위치 계산 직접 해보기
- [ ] 새로운 부서 색상 추가해보기
- [ ] 타임라인 범위를 2024-2030으로 변경해보기

---

#### 4-3. 미분류 기술계획 관리
📂 **읽을 파일**:
1. ✅ `src/pages/tech-plan/UnclassifiedTechPlan.tsx`
   - 테이블 뷰
   - 다중 필터 (부서, 상태, 검색어)
   - 분류 할당 다이얼로그

**핵심 로직**:
- **필터링 알고리즘**:
  ```typescript
  filteredItems = items.filter(item =>
    matchesSearch && matchesDepartment && matchesStatus
  );
  ```
- **Cascading Select**: 대분류 선택 → 중분류 옵션 변경 → 소분류 옵션 변경

**실습**:
- [ ] 검색어 "AI" 입력 시 필터링 과정 추적
- [ ] 분류 할당 다이얼로그의 Select 연쇄 반응 이해

---

#### 4-4. 부서 & 사용자 관리 (CRUD 패턴)
📂 **읽을 파일**:
1. ✅ `src/pages/admin/DepartmentManagement.tsx`
2. ✅ `src/components/admin/DepartmentManagement.tsx` ⭐ **CRUD 학습**

**핵심 기능**:
- **Create**: 다이얼로그 → 폼 입력 → 유효성 검사 → 상태 업데이트 → Toast
- **Read**: 부서별 사용자 수 집계, 테이블 렌더링
- **Update**: 기존 데이터 폼에 채우기 → 수정 → 상태 업데이트
- **Delete**: 확인 → 배열에서 제거 → Toast

**상태 관리 패턴**:
```typescript
const [users, setUsers] = useState(initialUsers);
const [isDialogOpen, setIsDialogOpen] = useState(false);
const [editingUser, setEditingUser] = useState(null);
```

**실습**:
- [ ] 새 사용자 추가 흐름 전체 추적
- [ ] 사용자 수정 시 폼 pre-fill 메커니즘 이해
- [ ] 삭제 시 상태 업데이트 방식 분석

---

#### 4-5. 권한 요청 시스템
📂 **읽을 파일**:
1. ✅ `src/pages/PermissionRequest.tsx`
   - 트리에서 체크박스로 다중 선택
   - 재귀적으로 선택된 항목 이름 수집
   - 폼 제출 및 Toast 알림

**핵심 알고리즘**:
```typescript
const getSelectedItemNames = (): string[] => {
  const names: string[] = [];
  const findNames = (nodes: ClassificationNode[]) => {
    nodes.forEach((node) => {
      if (selectedItems.includes(node.id)) {
        names.push(node.name);
      }
      if (node.children) findNames(node.children); // 재귀
    });
  };
  findNames(classificationTreeData);
  return names;
};
```

**실습**:
- [ ] 체크박스 선택 시 selectedItems 배열 변화 관찰
- [ ] 재귀 함수 실행 과정을 손으로 따라가기

---

### **5단계: UI 시스템 & 고급 패턴** (3-4일)
**목표**: shadcn/ui 컴포넌트 시스템과 재사용 패턴 마스터

#### 읽을 파일:
1. ✅ `src/lib/utils.ts` - `cn()` 함수 (className 병합)
2. ✅ `src/components/ui/button.tsx` - CVA 패턴 이해
3. ✅ `src/components/ui/dialog.tsx` - Radix UI Compound Component
4. ✅ `src/components/ui/select.tsx` - Controlled Component 패턴
5. ✅ `src/components/ui/table.tsx` - 테이블 컴포넌트 구조
6. ✅ `src/hooks/use-toast.ts` - Custom Hook 패턴

#### 핵심 개념:
- **Class Variance Authority (CVA)**: 버튼 variant 관리
  ```typescript
  buttonVariants = cva("base-classes", {
    variants: {
      variant: { default: "...", destructive: "..." },
      size: { sm: "...", lg: "..." }
    }
  });
  ```

- **Compound Component Pattern**:
  ```tsx
  <Dialog>
    <DialogTrigger />
    <DialogContent>
      <DialogHeader>
        <DialogTitle />
      </DialogHeader>
    </DialogContent>
  </Dialog>
  ```

- **Custom Hook Pattern**:
  ```typescript
  export function useToast() {
    const [toasts, setToasts] = useState([]);
    const toast = ({ title, description }) => { ... };
    return { toast, toasts, dismiss };
  }
  ```

#### 실습:
- [ ] Button에 새로운 variant 추가 (예: "gradient")
- [ ] Toast 메시지 커스터마이즈
- [ ] 새로운 UI 컴포넌트 만들기 (shadcn/ui 패턴 따라)

---

## 🎓 학습 후 도전 과제

### 초급 과제:
1. ⭐ Header에 새로운 탭 추가하기
2. ⭐ 타임라인에 새로운 부서 추가하고 색상 지정
3. ⭐ 분류 트리에 새로운 대분류 항목 추가

### 중급 과제:
1. ⭐⭐ 사용자 관리 페이지에 "역할 필터" 기능 추가
2. ⭐⭐ 타임라인에서 특정 부서만 보는 토글 버튼 구현
3. ⭐⭐ 미분류 기술계획에 "일괄 분류 할당" 기능

### 고급 과제:
1. ⭐⭐⭐ React Query로 실제 API 연동 구현
2. ⭐⭐⭐ 타임라인 Drag & Drop으로 날짜 변경
3. ⭐⭐⭐ TypeScript strict mode 활성화 후 타입 에러 해결

---

## 📖 핵심 개념 정리

### React 패턴:
- **Functional Components**: 모든 컴포넌트가 함수형
- **Hooks**: useState, useEffect, useContext, useCallback
- **Props Drilling**: 데이터를 부모→자식으로 전달
- **Lifting State Up**: 공통 상태를 부모로 올리기
- **Composition**: children props로 컴포넌트 조합

### TypeScript:
- **Interface**: 객체 구조 정의
- **Union Type**: `"A" | "B" | "C"` 제한된 선택지
- **Optional Properties**: `name?: string`
- **Generic Types**: `<T>` 재사용 가능한 타입

### Tailwind CSS:
- **Utility-First**: `className="flex items-center gap-2"`
- **Responsive**: `sm:`, `md:`, `lg:` breakpoints
- **Dark Mode**: `dark:bg-slate-800`
- **Custom Colors**: tailwind.config.ts에서 정의

### shadcn/ui:
- **Radix UI**: 접근성이 보장된 headless UI
- **CVA**: 컴포넌트 variant 관리
- **Compound Components**: 여러 컴포넌트를 조합
- **Controlled Components**: 외부 상태로 제어

---

## 🚀 학습 완료 후 할 수 있는 것

✅ 새로운 페이지 추가 (라우팅 포함)
✅ 새로운 데이터 모델 정의
✅ CRUD 기능 구현
✅ 복잡한 필터링/검색 로직 작성
✅ 타임라인 시각화 커스터마이즈
✅ shadcn/ui 컴포넌트 활용
✅ React Query로 API 연동
✅ TypeScript로 타입 안전성 확보

---

## 📚 추천 학습 자료

- **React 공식 문서**: https://react.dev
- **TypeScript 핸드북**: https://www.typescriptlang.org/docs
- **Tailwind CSS 문서**: https://tailwindcss.com
- **shadcn/ui 컴포넌트**: https://ui.shadcn.com
- **React Router**: https://reactrouter.com
- **TanStack Query**: https://tanstack.com/query

---

## ⏱️ 예상 학습 시간

| 단계 | 소요 시간 | 누적 시간 |
|------|----------|----------|
| 1단계: 프로젝트 기초 | 3-4일 | 4일 |
| 2단계: 레이아웃 & 내비게이션 | 2-3일 | 7일 |
| 3단계: 데이터 모델 | 2-3일 | 10일 |
| 4단계: 주요 기능 페이지 | 5-7일 | 17일 |
| 5단계: UI 시스템 | 3-4일 | 21일 |
| **총계** | **15-21일** | **3-4주** |

*하루 2-3시간 학습 기준*

---

## 💡 학습 팁

1. **순서를 지키세요**: 각 단계는 이전 단계 위에 쌓입니다
2. **코드를 실행하며 읽으세요**: 파일을 읽은 후 반드시 브라우저에서 동작 확인
3. **손으로 따라 쓰세요**: 복사/붙여넣기 말고 직접 타이핑
4. **개발자 도구 활용**: React DevTools로 컴포넌트 구조와 상태 확인
5. **작은 실험**: 코드 한 줄씩 바꿔보며 어떻게 동작하는지 관찰
6. **문서 참조**: 막히면 공식 문서를 읽어보세요
7. **질문하기**: 이해 안 되는 부분은 질문하세요!

---

**🎯 최종 목표 달성 기준:**
이 프로젝트를 완전히 이해했다면, "새로운 기능을 추가해달라"는 요청을 받았을 때 어떤 파일을 수정해야 하는지 즉시 알 수 있어야 합니다!
